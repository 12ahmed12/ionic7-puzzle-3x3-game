import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic7-puzzle',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
